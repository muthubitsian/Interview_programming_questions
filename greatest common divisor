#include<stdio.h>
int main()
{
    int x,y;
    x=20,y=15;
    
    while(x!=y)
    {
        if(x>y)
        x=x-y;
        else
        y=y-x;
    }
    printf("\"result\" :%d",x);
    
    
    return 0;
}
