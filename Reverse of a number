#include<stdio.h>

int main()
{
    int n;
    printf("enter the number");
    scanf("%d",&n);
    int result=0;
    while(n!=0)
    {
        result=result*10+n%10;
        n=n/10;
    }
    
    printf("the reverse number is:%d",result);
    return 0;
}
